node_modules/
dist/

.env*

# Local targate state — approvals.json is committed by design, these are not.
.targate/ai-cache.json
.targate/last-run.json
.targate/monitor-baseline.json

# Local-agent orchestration: run artifacts, worktrees, and worker temp files.
.local-agent-runs/
.local-agent-worktrees/
.la-worker-*.settings.json
